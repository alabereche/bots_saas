rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Product images: public read for customers, auth write for bot owners
    match /bots/{botId}/products/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
